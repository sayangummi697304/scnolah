-- === CONFIGURASI ===
local CONFIG = {
    TARGET_FPS = 144,
    TARGET_PING = 20,
    RESCAN_INTERVAL = 3, -- Lebih kerap untuk fix texture
    CLEANUP_INTERVAL = 60,
    DEBUG_MODE = false,
    BATCH_SIZE = 300,
    
    -- Force Ping Optimization
    FORCE_PING = {
        ENABLED = true,
        TARGET = 50,
        NETWORK_OPTIMIZE = true,
    },
    
    BALL_REACT = {
        ENABLED = true, -- Auto active, no hotkey needed
        REACT_DISTANCE = 2.8,
        REACT_FORCE = 150, -- DIUBAH KE 150
        COOLDOWN = 0.03,
        UPWARD_BOOST = 0.3,
        VELOCITY_INHERIT = 0.6,
        SPIN_FORCE = 15,
        AUTO_SCAN_INTERVAL = 2,
        BODY_MULTIPLIERS = {
            ["Head"] = 0.9,
            ["UpperTorso"] = 1.1,
            ["LowerTorso"] = 1.0,
            ["HumanoidRootPart"] = 1.2,
            ["LeftUpperArm"] = 0.7,
            ["RightUpperArm"] = 0.7,
            ["LeftLowerArm"] = 0.8,
            ["RightLowerArm"] = 0.8,
            ["LeftHand"] = 0.6,
            ["RightHand"] = 0.6,
            ["LeftUpperLeg"] = 1.3,
            ["RightUpperLeg"] = 1.3,
            ["LeftLowerLeg"] = 1.5,
            ["RightLowerLeg"] = 1.5,
            ["LeftFoot"] = 1.8,
            ["RightFoot"] = 1.8,
            ["Torso"] = 1.1,
            ["Left Arm"] = 0.7,
            ["Right Arm"] = 0.7,
            ["Left Leg"] = 1.5,
            ["Right Leg"] = 1.5,
        },
    },
}

--=== SERVICES ===--
local Services = {
    Lighting = game:GetService("Lighting"),
    Players = game:GetService("Players"),
    RunService = game:GetService("RunService"),
    Debris = game:GetService("Debris"),
    UserInputService = game:GetService("UserInputService"),
}

local LocalPlayer = Services.Players.LocalPlayer

--=== STATE ===--
local State = {
    modifiedParts = {},
    connections = {},
    protectedCache = {},
    isRunning = true,
}

--=== BALL STATE ===--
local BallState = {
    balls = {},
    ballSet = {},
    lastReact = {},
    bodyParts = {},
    reactCount = 0,
}

local BR = CONFIG.BALL_REACT

--=== UTILITIES ===--
local function debugPrint(...)
    if CONFIG.DEBUG_MODE then
        print("[DEBUG]", ...)
    end
end

local function safePcall(func, ...)
    local success, result = pcall(func, ...)
    return success, result
end

print("═══════════════════════════════════════════════════")
print("🚀 ULTRA OPTIMIZER + AUTO BALL REACT")
print("📝 Script made by Jamal with AI Claude")
print("⚡ FIXED: Texture Plastic + NO SHADOW")
print("⚽ AUTO BALL REACT: FORCE 150")
print("═══════════════════════════════════════════════════")

--=== FPS & LIGHTING (FORCE NO SHADOW) ===--
safePcall(function() setfpscap(CONFIG.TARGET_FPS) end)

safePcall(function()
    local L = Services.Lighting
    L.GlobalShadows = false
    L.FogEnd = 100000
    L.Brightness = 2
    L.ClockTime = 14
    L.Ambient = Color3.fromRGB(0, 0, 0)
    L.OutdoorAmbient = Color3.fromRGB(0, 0, 0)
    
    for _, v in pairs(L:GetChildren()) do
        if v:IsA("PostEffect") or v:IsA("Atmosphere") or v:IsA("Clouds") then
            v.Enabled = false
        end
    end
end)

-- Force Ping Optimization
if CONFIG.FORCE_PING.ENABLED then
    safePcall(function()
        print("📶 Enabling Network Ping Optimizer...")
        settings().Network.IncomingReplicationLag = 0
        settings().Network.OutgoingKBPSLimit = 0
        settings().Network.PhysicsSendRate = 60
        settings().Network.PhysicsReceiveRate = 60
        game:GetService("GuiService").GuiModifier = Enum.GuiModifier.NoModifier
    end)
end

--=== PROTECTION ===--
local function isPlayerRelated(obj)
    if not obj or not obj.Parent then return false end
    for _, player in pairs(Services.Players:GetPlayers()) do
        local char = player.Character
        if char and (obj == char or obj:IsDescendantOf(char)) then
            return true
        end
    end
    return false
end

local function isProtectedObject(obj)
    if not obj then return true end
    if State.protectedCache[obj] ~= nil then return State.protectedCache[obj] end
    if isPlayerRelated(obj) then State.protectedCache[obj] = true return true end
    
    local name = obj.Name:lower()
    for _, kw in ipairs({"ball", "bola", "soccer", "football", "goal", "net", "sphere"}) do
        if name:find(kw, 1, true) then
            State.protectedCache[obj] = true
            return true
        end
    end
    
    State.protectedCache[obj] = false
    return false
end

--=== OBJECT OPTIMIZER (ENHANCED FOR PLASTIC + NO SHADOW) ===--
local function optimizeObject(obj)
    if not obj or not obj.Parent then return end
    if isProtectedObject(obj) then return end
    
    safePcall(function()
        if obj:IsA("BasePart") and not obj:IsA("Terrain") then
            -- FORCE PLASTIC MATERIAL
            obj.Material = Enum.Material.Plastic
            -- FORCE NO SHADOW
            obj.CastShadow = false
            obj.Reflectance = 0
            
            -- FORCE remove textures
            if obj:IsA("MeshPart") then 
                obj.TextureID = "" 
            end
            obj.Transparency = math.max(0, obj.Transparency)
        end
        
        -- Remove visual effects
        if obj:IsA("Texture") or obj:IsA("Decal") then 
            obj.Transparency = 1 
        end
        if obj:IsA("SurfaceAppearance") then 
            obj:Destroy() 
        end
        if obj:IsA("ParticleEmitter") or obj:IsA("Trail") or obj:IsA("Fire") or
           obj:IsA("Smoke") or obj:IsA("Sparkles") or obj:IsA("Beam") then
            obj.Enabled = false
        end
    end)
    
    State.modifiedParts[obj] = true
end

--=== TERRAIN ===--
safePcall(function()
    local terrain = workspace:FindFirstChildOfClass("Terrain")
    if terrain then
        terrain.WaterWaveSize = 0
        terrain.WaterWaveSpeed = 0
        terrain.Decoration = false
    end
end)

--============================================--
--=== ⚽ AUTO BALL REACT SYSTEM (FORCE 150) ===--
--============================================--

local function isBallObject(obj)
    if not obj then return false end
    if not obj:IsA("BasePart") then return false end
    if isPlayerRelated(obj) then return false end
    
    local name = obj.Name:lower()
    local parentName = obj.Parent and obj.Parent.Name:lower() or ""
    
    local keywords = {"ball", "bola", "soccer", "football", "soccerball", 
                      "matchball", "gameball", "sepak", "bolo", "sphere"}
    
    for _, kw in ipairs(keywords) do
        if name:find(kw, 1, true) or parentName:find(kw, 1, true) then
            return true
        end
    end
    
    local isBall = false
    safePcall(function()
        if obj.Shape == Enum.PartType.Ball then isBall = true end
    end)
    if isBall then return true end
    
    local isRound = false
    safePcall(function()
        local s = obj.Size
        local minS, maxS = math.min(s.X, s.Y, s.Z), math.max(s.X, s.Y, s.Z)
        isRound = (maxS - minS) < 0.8 and maxS < 4 and maxS > 0.5
    end)
    
    return isRound
end

local function addBall(obj)
    if not obj or BallState.ballSet[obj] then return end
    if not isBallObject(obj) then return end
    
    BallState.ballSet[obj] = true
    table.insert(BallState.balls, obj)
    debugPrint("⚽ Ball added:", obj.Name)
    
    local conn
    conn = obj.AncestryChanged:Connect(function(_, parent)
        if not parent then
            BallState.ballSet[obj] = nil
            conn:Disconnect()
            debugPrint("⚽ Ball removed:", obj.Name)
        end
    end)
end

local function cleanBalls()
    for i = #BallState.balls, 1, -1 do
        local ball = BallState.balls[i]
        if not ball or not ball.Parent then
            BallState.ballSet[ball] = nil
            table.remove(BallState.balls, i)
        end
    end
end

local function fullBallScan()
    local found = 0
    for _, obj in pairs(workspace:GetDescendants()) do
        if isBallObject(obj) and not BallState.ballSet[obj] then
            addBall(obj)
            found = found + 1
        end
    end
    cleanBalls()
    if found > 0 then
        debugPrint("🔍 Scan found " .. found .. " new balls")
    end
    return #BallState.balls
end

local function updateBodyParts()
    BallState.bodyParts = {}
    local char = LocalPlayer.Character
    if not char then return end
    
    for _, part in pairs(char:GetDescendants()) do
        if part:IsA("BasePart") then
            table.insert(BallState.bodyParts, {
                part = part,
                multiplier = BR.BODY_MULTIPLIERS[part.Name] or 0.8,
            })
        end
    end
end

local function reactBall(ball, bodyData, distance)
    if not ball or not ball.Parent then return end
    if not bodyData or not bodyData.part.Parent then return end
    
    local part = bodyData.part
    local now = tick()
    local key = tostring(ball) .. part.Name
    
    if BallState.lastReact[key] and now - BallState.lastReact[key] < BR.COOLDOWN then
        return
    end
    
    local dir = (ball.Position - part.Position)
    if dir.Magnitude < 0.1 then dir = Vector3.new(0, 1, 0) end
    dir = dir.Unit
    
    local partVel = Vector3.zero
    safePcall(function() partVel = part.AssemblyLinearVelocity or Vector3.zero end)
    
    local distMult = math.clamp(1 - (distance / BR.REACT_DISTANCE), 0.3, 1)
    local force = dir * BR.REACT_FORCE * bodyData.multiplier * distMult
    local inherited = partVel * BR.VELOCITY_INHERIT
    local upBoost = Vector3.new(0, BR.REACT_FORCE * BR.UPWARD_BOOST, 0)
    local final = force + inherited + upBoost
    
    if final.Magnitude > 250 then final = final.Unit * 250 end -- Increased limit for 150 force
    
    safePcall(function() ball.AssemblyLinearVelocity = final end)
    safePcall(function() ball.Velocity = final end)
    
    safePcall(function()
        local spin = dir:Cross(Vector3.new(0, 1, 0))
        if spin.Magnitude > 0.1 then
            ball.AssemblyAngularVelocity = spin.Unit * BR.SPIN_FORCE
        end
    end)
    
    if firetouchinterest then
        safePcall(function()
            firetouchinterest(part, ball, 0)
            task.defer(function() firetouchinterest(part, ball, 1) end)
        end)
    end
    
    BallState.lastReact[key] = now
    BallState.reactCount = BallState.reactCount + 1
    debugPrint(string.format("⚽ React! Part: %s | Force: %.1f", part.Name, final.Magnitude))
end

local function startAutoReact()
    fullBallScan()
    updateBodyParts()
    
    workspace.DescendantAdded:Connect(function(obj)
        task.defer(function()
            if isBallObject(obj) then
                addBall(obj)
            end
        end)
    end)
    
    LocalPlayer.CharacterAdded:Connect(function()
        task.wait(0.3)
        updateBodyParts()
    end)
    
    task.spawn(function()
        while State.isRunning do
            task.wait(BR.AUTO_SCAN_INTERVAL)
            fullBallScan()
        end
    end)
    
    Services.RunService.Heartbeat:Connect(function()
        if not BR.ENABLED or not State.isRunning then return end
        
        local char = LocalPlayer.Character
        if not char then return end
        
        if #BallState.bodyParts == 0 then updateBodyParts() end
        
        local hrp = char:FindFirstChild("HumanoidRootPart")
        if not hrp then return end
        local hrpPos = hrp.Position
        
        for _, ball in ipairs(BallState.balls) do
            if not ball or not ball.Parent then continue end
            
            local roughDist = (ball.Position - hrpPos).Magnitude
            if roughDist > BR.REACT_DISTANCE * 3 then continue end
            
            local closest, closestDist = nil, math.huge
            
            for _, bodyData in ipairs(BallState.bodyParts) do
                if not bodyData.part.Parent then continue end
                local dist = (ball.Position - bodyData.part.Position).Magnitude
                if dist < closestDist then
                    closestDist = dist
                    closest = bodyData
                end
            end
            
            if closest and closestDist <= BR.REACT_DISTANCE then
                reactBall(ball, closest, closestDist)
            end
        end
    end)
end

task.spawn(startAutoReact)

--============================================--
--=== OPTIMIZER LOOPS (ENHANCED SCANNING) ===--
--============================================--

-- Initial optimization
task.spawn(function()
    task.wait(0.5)
    for i, obj in ipairs(workspace:GetDescendants()) do
        optimizeObject(obj)
        if i % CONFIG.BATCH_SIZE == 0 then task.wait() end
    end
end)

-- Optimize new objects
workspace.DescendantAdded:Connect(function(obj)
    task.wait(0.1)
    optimizeObject(obj)
end)

-- FORCE OPTIMIZE EVERY 3 SECONDS (fix texture reset + shadow)
task.spawn(function()
    while State.isRunning do
        task.wait(CONFIG.RESCAN_INTERVAL)
        for _, obj in pairs(workspace:GetDescendants()) do
            if obj:IsA("BasePart") then
                optimizeObject(obj)
            end
        end
        debugPrint("🔄 Force Plastic + No Shadow Scan Complete")
    end
end)

-- Cleanup
task.spawn(function()
    while State.isRunning do
        task.wait(CONFIG.CLEANUP_INTERVAL)
        for obj in pairs(State.modifiedParts) do
            if typeof(obj) == "Instance" and not obj.Parent then
                State.modifiedParts[obj] = nil
                State.protectedCache[obj] = nil
            end
        end
        local now = tick()
        for key, time in pairs(BallState.lastReact) do
            if now - time > 5 then
                BallState.lastReact[key] = nil
            end
        end
        safePcall(collectgarbage, "collect")
    end
end)

--=== CREDIT MESSAGE ===--
task.spawn(function()
    while State.isRunning do
        task.wait(180)
        print("═══════════════════════════════════════════════════")
        print("📝 Script made by Jamal with AI Claude")
        print("═══════════════════════════════════════════════════")
    end
end)

--=== HOTKEY (Hanya Debug) ===--
safePcall(function()
    Services.UserInputService.InputBegan:Connect(function(input, processed)
        if processed then return end
        if input.KeyCode == Enum.KeyCode.F9 then
            CONFIG.DEBUG_MODE = not CONFIG.DEBUG_MODE
            print("🔧 Debug:", CONFIG.DEBUG_MODE and "ON" or "OFF")
        end
    end)
end)

--=== STOP FUNCTION ===--
if getgenv then
    getgenv().StopOptimizer = function()
        State.isRunning = false
        BR.ENABLED = false
        print("🛑 Stopped")
    end
end

--=== FINAL OUTPUT ===--
print("═══════════════════════════════════════════════════")
print("✅ FULL AUTO MODE AKTIF!")
print("═══════════════════════════════════════════════════")
print("🎯 FPS: " .. CONFIG.TARGET_FPS)
print("📶 Ping Optimizer: " .. (CONFIG.FORCE_PING.ENABLED and "ON" or "OFF"))
print("⚽ Ball React: AUTO ON (FORCE 150)")
print("📏 React Distance: " .. BR.REACT_DISTANCE .. " studs")
print("💪 React Force: " .. BR.REACT_FORCE)
print("🛡️ Plastic Material + NO Shadow: FORCE ACTIVE")
print("═══════════════════════════════════════════════════")
print("⌨️ F9 = Debug Mode")
print("═══════════════════════════════════════════════════")
